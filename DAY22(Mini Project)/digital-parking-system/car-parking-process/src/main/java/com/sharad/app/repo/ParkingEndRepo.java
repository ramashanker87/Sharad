package com.sharad.app.repo;

import com.sharad.app.module.ParkingEnd;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParkingEndRepo extends JpaRepository<ParkingEnd, Integer> {
}